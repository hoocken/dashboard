services:
  frontend:
    image:
      hoocken/dashboard
    build:
      target: dashboard
    ports:
      - "3000:3000"
  # backend:
  #   build:
  #     target: backend-dev
  #   ports:
  #     - "5050:5050"